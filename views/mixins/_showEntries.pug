mixin showEntries(entries)

    .entries
        //- pre #{entries}
    if entries.length
        each entry in entries
            h3 #{entry.name}
            hr
            p #{entry.description}
            p.browser= `Entry was made in ${entry.browser}`
    else
        p= 'No Data to Display'
        